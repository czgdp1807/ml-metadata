--- bazel/build_defs.bzl.orig
+++ bazel/build_defs.bzl
@@ -40,7 +40,6 @@
 _DEFAULT_COPTS.extend([
     "-std=c99",
     "-pedantic",
-    "-Werror=pedantic",
     "-Wall",
     "-Wstrict-prototypes",
     # GCC (at least) emits spurious warnings for this that cannot be fixed
